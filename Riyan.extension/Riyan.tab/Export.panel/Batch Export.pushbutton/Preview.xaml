<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sheet Preview" Height="800" Width="1000" WindowStartupLocation="CenterScreen" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    
    <Window.Resources>
        <!-- Close Button Style -->
        <Style x:Key="CloseBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="#802F2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0,10,0,0">
                            <TextBlock Text="&#x2715;" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}" FontSize="12"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#A13634"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#802F2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main Window Border -->
    <Border Background="#F9FAFB" CornerRadius="10" BorderBrush="#E5E7EB" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
            
            <!-- Title Bar -->
            <Border Grid.Row="0" Background="#FFFFFF" CornerRadius="10,10,0,0" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseDown"/>
            <Grid Grid.Row="0" MouseLeftButtonDown="TitleBar_MouseDown" Background="Transparent">
                <TextBlock x:Name="TxtTitle" Text="Previewing: " Foreground="#1F2937" FontSize="14" Margin="15,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <Button x:Name="BtnClose" Style="{StaticResource CloseBtnStyle}" Width="45" Height="40" HorizontalAlignment="Right" Click="BtnClose_Click"/>
            </Grid>
            
            <!-- Content -->
            <ScrollViewer x:Name="ScrollViewerMain" Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="#D1D5DB">
                <Image x:Name="ImgPreview" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
                    <Image.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="15" ShadowDepth="4"/>
                    </Image.Effect>
                    <Image.LayoutTransform>
                        <ScaleTransform x:Name="ImgScale" ScaleX="1" ScaleY="1"/>
                    </Image.LayoutTransform>
                </Image>
            </ScrollViewer>
            
            <!-- Bottom Bar -->
            <Border Grid.Row="2" Background="#FFFFFF" CornerRadius="0,0,10,10" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0" VerticalAlignment="Center">
                    <TextBlock Text="Zoom:" Foreground="#4B5563" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="13" FontWeight="SemiBold"/>
                    <Slider x:Name="SliderZoom" Width="200" Minimum="0.05" Maximum="5.0" Value="1.0" VerticalAlignment="Center" ValueChanged="SliderZoom_ValueChanged" Margin="0,0,20,0"/>
                    
                    <Button x:Name="BtnFit" Style="{StaticResource PrimaryBtn}" Content="Fit Page" Width="90" Height="32" Margin="0,0,10,0" Click="BtnFit_Click" Cursor="Hand"/>
                    <Button x:Name="BtnActual" Style="{StaticResource PrimaryBtn}" Content="100%" Width="70" Height="32" Margin="0,0,0,0" Click="BtnActual_Click" Cursor="Hand"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>