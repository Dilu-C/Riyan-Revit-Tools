<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Riyan - Door/Window Legend Generator" Height="240" Width="480"
        WindowStartupLocation="CenterScreen" Background="#F9FAFB"
        FontFamily="Segoe UI">
    <Window.Resources>
        <!-- Modern Button Style -->
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Legend Generator" FontSize="22" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,15" Grid.Row="0"/>
        
        <TextBlock Text="Select Category:" FontWeight="SemiBold" Foreground="#4B5563" Margin="0,0,0,5" Grid.Row="1"/>
        <ComboBox Name="ComboCategory" Grid.Row="2" SelectedIndex="0" Margin="0,0,0,15" Height="30" Padding="8,4" Background="White" BorderBrush="#D1D5DB">
            <ComboBoxItem Content="Doors"/>
            <ComboBoxItem Content="Windows"/>
        </ComboBox>
        
        <TextBlock Name="TxtProgress" Text="" Grid.Row="3" Foreground="#059669" FontWeight="Medium" TextWrapping="Wrap" VerticalAlignment="Top"/>
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="4">
            <Button Name="BtnCancel" Content="Cancel" Width="90" Height="32" Margin="0,0,10,0" Background="White" Foreground="#374151" BorderThickness="1" BorderBrush="#D1D5DB" FontWeight="SemiBold"/>
            <Button Name="BtnGenerate" Content="Generate" Width="130" Height="32" Background="#2563EB" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>